# Test fixtures

`filters_annual_statistics_2025_26.catalog` is a small filter-only catalog used
by `tests/testthat/test_pipeline.R`. It verifies the complete filter branch and
confirms that saved-column outputs are skipped when no saved columns exist.
